# Introduction_to_OOP

## Модуль Product|Category

В данных модулях отображены классы, также произведена инициализацию.

- у класса Product определены атрибуты: name, description, price, quantity.
- у класса Category имеет два атрибута класса: количество категорий (count_category) и количество товаров (
  count_product).

## Задание 14.2

- В данном модуле мы реализовываем работу с приватными атрибутами, к которым нельзя обращаться от объекта, но можно
  обращаться внутри класса;
- Используем декоратор property для создания геттера с выводом списка товаров;
- В класс-методе класса Product необходимо вызывать метод-конструктор __init__  от класса продуктов, а созданный объект
  возвращать как результат работы метода;
- Работа с геттерами и сеттерами. Мы защищаем пользователя от случайного введения некорректной цены.

## Тестирование.

Проект покрыт юнит-тестами Pytest.
Чтобы запустить тесты с оценкой покрытия выполните команду: pytest

## Задание 14.2

- В данном модуле мы реализовываем работу с приватными атрибутами, к которым нельзя обращаться от объекта, но можно
  обращаться внутри класса;
- Используем декоратор property для создания геттера с выводом списка товаров;
- В класс-методе класса Product необходимо вызывать метод-конструктор __init__  от класса продуктов, а созданный объект
  возвращать как результат работы метода;
- Работа с геттерами и сеттерами. Мы защищаем пользователя от случайного введения некорректной цены.

## Задание 15.3 (Ветка feature 14_3)

- В Модулях реализовали строковое отображение:
  ***Название продукта, 80 руб. Остаток: 15 шт.***
  ***Название категории, количество продуктов: 200 шт.***
- Добавил магический метод _add

## Задание 16.1 (Ветка feature 16_1)

- Созданы 2 класса (***Smartphone, LawnGrass***)
- Доработайте функциональность сложения таким образом, чтобы можно было складывать товары только из одинаковых классов
  продуктов.
- Доработан метод, который добавляет продукт в категорию, таким образом, чтобы не было возможности добавить вместо
  продукта или его наследников любой другой объект. (***#isinstance #issubclass***)